const isEven = (num) => {
    return num % 2 === 0;
};

const double = (num) => {
    return num * 2;
};

const square = (num) => {
    return num * num;
};

module.exports = { isEven, double, square };
